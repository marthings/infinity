<section class="capture-page">
  <%= native_form_tag %>
  <%= native_navbar_tag("Edit capture") %>

  <header class="page-header">
    <h1 class="native-hidden">Edit capture</h1>
    <%= link_to "Back to capture", @capture %>
  </header>

  <%= render "form", capture: @capture %>
</section>
