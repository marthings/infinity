<section class="capture-page">
  <%= native_form_tag %>
  <%= native_navbar_tag("New capture") %>

  <header class="page-header">
    <h1 class="native-hidden">New capture</h1>
    <%= link_to "Back to captures", captures_path %>
  </header>

  <%= render "form", capture: @capture %>
</section>
