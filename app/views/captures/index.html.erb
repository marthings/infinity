<header class="home-hero native-inset-top">
    <h1>Save it</h1>
    <%= render "quick_form", capture: @capture %>
</header>

<section class="capture-page native-home-page">
    <%= native_navbar_tag do |navbar| %>
        <%= navbar.button icon: "plus", href: new_capture_path %>
    <% end %>
    <%= turbo_stream_from [ Current.user, :captures ] %>

    <% if @captures.any? %>
        <div class="capture-list">
            <%= render @captures %>
        </div>
    <% else %>
        <p>No captures yet.</p>
    <% end %>
</section>
