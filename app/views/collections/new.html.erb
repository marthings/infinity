<section class="capture-page">
  <%= native_form_tag %>
  <%= native_navbar_tag("New collection") %>

  <header class="page-header">
    <h1 class="native-hidden">New collection</h1>
    <%= link_to "Back to collections", collections_path, class: "native-hidden" %>
  </header>

  <%= render "form", collection: @collection %>
</section>
