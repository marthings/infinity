<section class="capture-page">
  <%= native_form_tag %>
  <%= native_navbar_tag("Edit tag") %>

  <header class="page-header">
    <h1 class="native-hidden">Edit tag</h1>
    <%= link_to "Back to tag", @tag, class: "native-hidden" %>
  </header>

  <%= render "form", tag: @tag %>
</section>
